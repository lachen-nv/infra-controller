# Phase 2 path-trigger smoke marker — core-only

Throwaway file for #1756 Phase 2.2 validation. Touching only a root-level file so the rest gate skips and the core gate runs end-to-end. Delete after validation.
