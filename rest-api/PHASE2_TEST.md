# Phase 2 path-trigger smoke marker — rest-only

Throwaway file for #1756 Phase 2.1 validation. Touching only `rest-api/**` so the core gate skips and the rest gate runs end-to-end. Delete after validation.
